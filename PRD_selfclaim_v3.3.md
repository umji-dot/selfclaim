# 셀프클레임 V3.3 — 제품 기능 명세서 (PRD)

> 분석 대상: `/Users/ims_connect/Documents/GitHub/selfclaim/c.html`
> 디자인 요소(색상/폰트/간격/아이콘 등)는 분석 범위에서 제외하고, 기능·구조·플로우·비즈니스 로직만 기술합니다.

---

## 1. 서비스 개요

### 1.1 서비스 목적
자동차 사고 발생 직후, **보험 가입 고객이 상담원 통화 없이 챗봇 대화 흐름만으로** 다음 두 가지를 셀프 처리할 수 있도록 한다.
1. **렌터카(대차) 신청** — 사고 차량 수리 기간 동안 사용할 대체 차량을 협력 렌터카 업체에 배정 요청
2. **예상 교통비 조회(시뮬레이션)** — 렌터카를 이용하지 않을 경우 받게 되는 일일 교통비를 과실비율·수리기간 기준으로 안내

### 1.2 주요 사용자
- **1차 사용자**: DB손해보험의 자동차보험 가입 고객 중 사고 접수 직후 보상 처리를 시작한 가입자/피보험자
- **사용 맥락**: 사고 직후의 모바일 환경(스트레스 상황). 짧은 시간 안에 의사결정 필요
- **사용 기기**: 모바일 단말(최대 폭 430px 기준의 단일 컬럼 챗 UI)

### 1.3 핵심 가치
| 가치 | 설명 |
|---|---|
| 무인 처리 | 상담원 호출 없이 챗봇 대화만으로 렌트/교통비 처리 완료 |
| 정황 기반 안내 | 차량번호 1개 입력으로 차종·연식·배기량 자동 조회 → 입력 최소화 |
| 의사결정 비교 지원 | "렌트 vs 교통비" 두 경로를 한 화면에서 비교 후 변경 가능 |
| 즉시 완결성 | 입력 → 확정 → 알림톡 발송까지 단일 세션에서 마감 |

### 1.4 해결하려는 문제
1. **상담원 통화 적체** — 사고 직후 상담 콜 수요가 폭증해 대기 시간이 길어지는 문제
2. **렌트/교통비 선택의 정보 비대칭** — 고객이 두 옵션을 비교하지 못하고 한 쪽을 선택하는 문제
3. **반복 입력 비용** — 차량번호 외에도 차량명·연식·배기량을 매번 다시 묻는 비효율
4. **수리점 탐색 부담** — 어디서 수리할지 정하지 못한 고객을 위한 위치 기반 협력 공업사 추천 부재
5. **약관 확인 부담** — 다수의 약관을 한 번에 보여 이탈을 유발하는 구조 → 필수 3건으로 간소화

---

## 2. 정보구조(IA)

### 2.1 메뉴 구조
본 서비스는 일반적인 메뉴/네비게이션이 아닌, **단일 챗봇 세션(linear conversation)**으로 구성된다. 메뉴 트리 대신 **대화 단계(Step)** 기반의 흐름이 IA를 이룬다.

### 2.2 사이트맵(상태 노드 다이어그램)

```
[Root] 셀프클레임 V3.3 챗봇 세션
│
├── S0. 헤더 영역 (전체 세션 공통)
│   ├── 로고
│   ├── 차량 정보 바 (vbar) — 조회 완료 후 노출
│   │   └── ✎ 차량 정보 직접 수정 (→ Sheet A 재호출)
│   └── 종료 버튼
│       └── 종료 확인 다이얼로그 (E1)
│
├── S1. 인트로 메시지
│
├── S2. 차량번호 입력 카드 (PlateInputCard)
│   ├── 정규식 검증: ^\d{2,3}[가-힣]\d{4}$
│   ├── (정상) 차량 자동 조회 → S3
│   └── (조회 실패: 99 시작 번호) → Sheet A (차량 정보 직접 입력)
│
├── S3. 차량 정보 확인 메시지
│
├── S4. 메인 분기 선택 (QuickChoice)
│   ├── [A] 렌터카 신청 → S6
│   └── [B] 예상 교통비 조회 → S5
│
├── S5. 교통비 시뮬레이션 서브플로우
│   ├── S5-1. 예상 수리 기간 선택 (Sheet B / 1~25일)
│   ├── S5-2. 예상 과실 비율 선택 (Sheet B 재활용 / 0,10,20,…,100%)
│   ├── S5-3. 일일 예상 교통비 산출 결과 표시
│   └── S5-4. 분기 재선택
│       ├── 렌터카 신청으로 변경 → S6
│       └── 나가기 → 종료 인사 (End)
│
├── S6. 지역 선택 (Sheet C)
│   ├── 서울특별시(25개구) 칩
│   └── 그 외 시(부산·대구·인천 등) 헤더만 노출
│
├── S7. 휴대폰 본인확인 (Inline Form: 연락처)
│   ├── 휴대폰 입력 → 인증번호 발송
│   ├── 인증번호 확인 (180초 타이머)
│   └── 입력 완료
│
├── S8. 추가 요청사항 (QuickChoice)
│   ├── 없어요 → S9
│   └── 있어요 → Inline Form (자유 텍스트 ≤100자) → S9
│
├── S9. 수리점 선택 분기 (QuickChoice)
│   ├── [A] 정해졌어요 → S9-A. 공업사명 직접 입력(≤30자)
│   └── [B] 못 정했어요 → S9-B. 협력 공업사 카드 리스트
│       ├── 상위 2곳 노출 + "더 보기"로 전체 노출
│       ├── 카드 선택 → 선택 확정 버튼
│       └── 지역 변경하기 → Sheet C 재호출 후 리스트 갱신
│
├── S10. 예약 정보 카드 노출 (S9-B 선택 시)
│
├── S11. 진행 힌트 + 필수 약관 동의 (Sheet D)
│   ├── 그룹1: 이용 자격 및 약관 (2건)
│   ├── 그룹2: 이용 제한 안내 확인 (1건)
│   └── 전체 동의 토글
│
└── S12. 완료 카드 (CompletionCard)
    └── 알림톡 발송 안내(메시지로만 안내)
```

### 2.3 화면 간 관계
- **선형(Linear)** 흐름이 기본. 단, S5 종료 시점·S9-B 내 "지역 변경" 등 일부 분기는 이전 단계로 우회 후 자동 복귀.
- **모달/시트(Bottom Sheet)**는 챗 흐름을 일시 가리고, 닫히면 챗 다음 메시지가 자동 진행.
- **헤더 차량 바**는 모든 단계(S3 이후)에 표시되며 ✎ 버튼으로 언제든 차량 정보 수정 가능.

### 2.4 사용자 이동 경로 (대표 3종)
1. **Happy Path A (렌트 신청, 수리점 미정)**: S1 → S2 → S3 → S4(렌트) → S6 → S7 → S8 → S9(못 정함) → S9-B 카드 선택 → S10 → S11 → S12
2. **Happy Path B (렌트 신청, 수리점 정함)**: S1 → S2 → S3 → S4(렌트) → S6 → S7 → S8 → S9(정함) → S9-A 입력 → S11 → S12
3. **Path C (교통비 조회 후 종료)**: S1 → S2 → S3 → S4(교통비) → S5 → S5-4(나가기) → End

---

## 3. 화면 정의

> 디자인 요소는 무시하고, 화면(메시지 단위 카드/모달) 기준으로 정의합니다.

### 화면 3.1 — 인트로 (S1)
- **화면 목적**: 사고 직후 고객을 정서적으로 안정시키고, 첫 입력(차량번호) 안내
- **진입 조건**: 세션 시작
- **주요 기능**: 봇 메시지 2회 자동 출력
- **사용자 액션**: 없음 (자동 진행)
- **이탈 경로**: 헤더의 [종료] → E1 다이얼로그
- **필요 데이터**: 없음

### 화면 3.2 — 차량번호 입력 카드 (S2)
- **화면 목적**: 차량번호 수집 후 차량 정보 자동 조회
- **진입 조건**: S1 인트로 완료
- **주요 기능**:
  - 차량번호 정규식 실시간 검증
  - [차량 조회하기] 버튼으로 명시적 제출
  - 조회 중 로딩 상태
  - 조회 실패 시 직접 입력 시트(Sheet A)로 폴백
- **사용자 액션**: 차량번호 입력 → 버튼 탭
- **이탈 경로**: 헤더 [종료]
- **필요 데이터**: 차량번호(plate)

### 화면 3.3 — 차량 정보 확인 (S3)
- **화면 목적**: 자동 조회된 차량 정보(번호·차명·차종·배기량·연식) 확인
- **진입 조건**: 차량 조회 성공 또는 Sheet A 입력 완료
- **주요 기능**: 봇 메시지로 차량 4개 필드 일괄 표시 + 헤더 차량 바 노출
- **사용자 액션**: 헤더 차량 바의 ✎ 버튼으로 수정 가능 (전 단계 어디서나)
- **이탈 경로**: 헤더 [종료]
- **필요 데이터**: plate, name, cls(차종), cc, year

### 화면 3.4 — 메인 분기 선택 (S4)
- **화면 목적**: 렌터카 신청 vs 예상 교통비 조회 선택
- **진입 조건**: S3 완료
- **주요 기능**: QuickChoice 2버튼
- **사용자 액션**: 버튼 1개 선택
- **이탈 경로**: 선택 결과에 따라 S5 또는 S6
- **필요 데이터**: branch 값('rent' | 'end')

### 화면 3.5 — 수리 기간 선택 시트 (S5-1, Sheet B)
- **화면 목적**: 예상 수리 기간(일) 수집
- **진입 조건**: S4에서 '교통비 조회' 선택
- **주요 기능**: 1~25일 칩 단일 선택 + 안내문(수리 가능 25일/불가능 10일/160h 초과 시 30일)
- **사용자 액션**: 칩 선택 → [완료]
- **이탈 경로**: [X] 닫기 또는 [완료]
- **필요 데이터**: days(정수, 1~25)

### 화면 3.6 — 과실 비율 선택 시트 (S5-2)
- **화면 목적**: 예상 과실 비율 수집
- **진입 조건**: S5-1 완료
- **주요 기능**: 0/10/20/…/100 (총 11개) 단일 선택 (0은 '무과실' 표기)
- **사용자 액션**: 칩 선택 → [완료]
- **이탈 경로**: [X] 닫기 또는 [완료]
- **필요 데이터**: faultRate(정수, 0|10|…|100)

### 화면 3.7 — 교통비 결과 메시지 (S5-3)
- **화면 목적**: 일일 예상 교통비 노출
- **진입 조건**: S5-2 완료
- **주요 기능**: `daily = 32,000 × (100 - faultRate)/100` 산출 결과 표시
- **사용자 액션**: 없음
- **이탈 경로**: S5-4로 자동 진행
- **필요 데이터**: daily 금액

### 화면 3.8 — 교통비 후 분기 (S5-4)
- **화면 목적**: 시뮬레이션 후 렌트 전환/종료 결정
- **사용자 액션**: '렌터카 신청으로 변경' / '나가기'
- **이탈 경로**: 'rent' → S6, 'exit' → End

### 화면 3.9 — 지역 선택 시트 (S6, Sheet C)
- **화면 목적**: 렌트 차량 인도 지역(구 단위) 선택
- **진입 조건**: S4 직접 또는 S5-4 우회 진입
- **주요 기능**:
  - 서울 25개구 칩 단일 선택
  - 시 단위(부산/대구/인천)는 헤더만 노출(미구현 placeholder)
- **사용자 액션**: 칩 선택 → [이 지역으로 선택]
- **필요 데이터**: region(문자열, 예: "광진구")

### 화면 3.10 — 연락처 입력 폼 (S7)
- **화면 목적**: 배송 시 연락받을 휴대폰 본인 확인
- **주요 기능**:
  - 전화번호 입력 → [인증번호 받기]
  - 6자리 인증번호 입력 → [확인]
  - 180초 카운트다운, 재발송 가능
  - 인증 성공 시 [입력 완료] 활성화
- **사용자 액션**: 입력·발송·확인·완료
- **필요 데이터**: phone(문자열)

### 화면 3.11 — 추가 요청사항 (S8)
- **화면 목적**: 렌터카 업체 전달용 자유 요청사항 수집(선택)
- **주요 기능**: '없어요'/'있어요' QuickChoice → 텍스트 영역(≤100자)
- **필요 데이터**: requestNote(선택, ≤100자)

### 화면 3.12 — 수리점 선택 분기 (S9)
- **화면 목적**: 수리점 정함/미정 분기
- **사용자 액션**: 'yes'/'no'

### 화면 3.13 — 협력 공업사 카드 리스트 (S9-B)
- **화면 목적**: 위치 기반 협력 공업사 추천 및 선택
- **주요 기능**:
  - 지역 기반 추천(현재 4건 mock, 상위 2건 + 더 보기)
  - 카드 단일 선택 → 선택 버튼 노출
  - [지역 변경하기]로 Sheet C 재호출 후 리스트 갱신
- **필요 데이터**: 공업사 객체(name, addr, dist, spec, phone)

### 화면 3.14 — 공업사명 직접 입력 (S9-A)
- **화면 목적**: 고객이 이미 정한 공업사명 수기 입력
- **주요 기능**: 텍스트 입력(≤30자)
- **필요 데이터**: shopName(필수, ≤30자)

### 화면 3.15 — 예약 정보 카드 (S10)
- **화면 목적**: 추천 수리점 선택 시 예약 요약(공업사명/주소/연락처)
- **진입 조건**: S9-B에서 카드 선택 완료
- **필요 데이터**: 선택된 shop 객체

### 화면 3.16 — 약관 동의 시트 (S11, Sheet D)
- **화면 목적**: 렌터카 신청 전 필수 약관 일괄 동의
- **주요 기능**:
  - 그룹1: 만 21세 이상 / 이용약관·개인정보
  - 그룹2: 이용 제한 안내 확인(가해자 렌트 불가, 교통비 중복 수급 불가, 대리기사 사고 렌트 불가)
  - 전체 동의 토글
  - 모든 필수 체크 시에만 [렌트 신청하기] 활성화
- **필요 데이터**: agree[0..2] : boolean

### 화면 3.17 — 완료 카드 (S12)
- **화면 목적**: 신청 완료 안내(지역·수리점 요약, 알림톡 발송 예고)
- **필요 데이터**: region, shopData.name, 안내문 fixed text

### 화면 3.18 — 종료 확인 다이얼로그 (E1)
- **화면 목적**: 헤더 [종료] 클릭 시 의도 재확인
- **사용자 액션**: 계속 진행 / 종료(페이지 새로고침)

### 화면 3.19 — 차량 정보 직접 입력 시트 (Sheet A)
- **화면 목적**: 차량번호 조회 실패 또는 ✎ 수정 시 4개 필드 수동 입력
- **필요 데이터**: plate, name, cls, cc, year

---

## 4. 기능 명세

### F-01 차량번호 입력 및 자동 조회
- **설명**: 사용자가 입력한 차량번호로 차량 정보를 자동 조회한다.
- **사용자 행동**: 차량번호 입력 후 [차량 조회하기] 탭
- **시스템 동작**: 정규식 `^\d{2,3}[가-힣]\d{4}$` 검증 → 로딩 1.8s → 차량 정보(name/cls/cc/year) 반환 → 헤더 차량 바 노출
- **비즈니스 규칙**:
  - 공백 자동 제거
  - 최대 길이 10자
  - 정규식 미통과 시 버튼 비활성
- **예외 처리**:
  - 정규식 실패: 안내 메시지(예: "23나4567, 123가1234")
  - 조회 실패(목업: `99`로 시작): 직접 입력 시트로 전환

### F-02 차량 정보 직접 입력/수정
- **설명**: 차량번호로 조회 실패 시 또는 ✎ 버튼으로 수동 입력
- **사용자 행동**: 4개 필드(차량명·차종·엔진크기·출고시기) 입력/선택
- **시스템 동작**: 모든 필드 입력 시 [입력 완료] 활성 → 시트 닫고 챗 흐름 복귀
- **비즈니스 규칙**: 4개 필드 모두 필수
- **예외 처리**: 일부 미입력 시 완료 버튼 비활성

### F-03 메인 플로우 분기
- **설명**: 렌터카 신청 vs 교통비 조회 선택
- **시스템 동작**: 'rent' → 렌트 메인 플로우, 'end' → 시뮬레이션 서브플로우
- **비즈니스 규칙**: 시뮬레이션 후에도 렌트로 전환 가능

### F-04 예상 교통비 계산
- **설명**: 과실 비율과 수리 기간을 입력받아 일일 교통비 산출
- **시스템 동작**: `daily = round(32000 × (100 - faultRate)/100)`
- **비즈니스 규칙**:
  - 기준 단가(32,000원/일)는 시스템 상수(현재 하드코딩)
  - 수리 기간(days)은 표시용이며 일일 단가 산식에는 영향 없음
  - 무과실(0)은 '무과실'로 표기, 그 외는 'N%'
- **예외 처리**: 정확한 금액은 보험사 담당자 확인 안내 문구 고정 노출

### F-05 지역 선택
- **설명**: 렌트 차량 인도 지역 단일 선택
- **비즈니스 규칙**:
  - 서울 25개구만 선택 가능(현 목업)
  - 부산/대구/인천은 헤더만 노출(미구현)
- **예외 처리**: 선택 없으면 [이 지역으로 선택] 비활성

### F-06 휴대폰 본인확인 (SMS OTP)
- **설명**: 연락처 입력 + SMS 인증번호 검증
- **시스템 동작**:
  - [인증번호 받기] → 입력 코드 필드 활성 + 180초 타이머
  - [확인] → 검증 통과 시 [입력 완료] 활성
- **비즈니스 규칙**:
  - 타이머 만료 후 재발송 가능
  - 인증 완료 후에만 다음 단계 진행
- **예외 처리(요구사항)**:
  - 잘못된 번호 형식
  - 인증번호 불일치
  - 시간 초과

### F-07 추가 요청사항
- **설명**: 자유 텍스트(≤100자) 수집(선택)
- **비즈니스 규칙**: 미입력 가능(없어요 선택 시 스킵)

### F-08 수리점 선택 분기
- **설명**: 정해짐/미정에 따라 입력 vs 추천
- **사용자 행동**: 'yes' 입력형 / 'no' 추천형

### F-09 협력 공업사 추천
- **설명**: 선택한 구 기준 협력 공업사 리스트 노출
- **시스템 동작**:
  - 상위 2건 우선 노출 + [더 보기]로 나머지 노출
  - 카드 선택 → 선택 버튼 노출
  - [지역 변경하기] → Sheet C 재호출 후 리스트 새로 그림
- **비즈니스 규칙**:
  - 카드 단일 선택
  - 추천 데이터는 지역 prefix 기반으로 동적 생성(현재 mock)
- **예외 처리(요구사항)**: 추천 결과 0건 처리(현재 미구현)

### F-10 공업사명 직접 입력
- **설명**: 사용자가 정한 공업사명 입력
- **비즈니스 규칙**: 1~30자, 공백만 입력 시 제출 불가

### F-11 약관 동의
- **설명**: 필수 3건 동의 후 신청 확정
- **시스템 동작**: 전체 동의 토글, 개별 체크 시 전체 체크 자동 동기화
- **비즈니스 규칙**: 모든 항목 필수, 미동의 시 [렌트 신청하기] 비활성
- **예외 처리(요구사항)**: 미동의 시 신청 불가, 약관 본문 보기(현재 미구현)

### F-12 신청 완료/알림톡
- **설명**: 신청 정보 요약 + 알림톡 발송 안내
- **비즈니스 규칙**:
  - 렌트 차량 자차/종합보험(대인·대물·자손) 가입 사실 고지
  - 취소·변경은 배정 업체에 직접 문의 고지
- **예외 처리(요구사항)**: 알림톡 발송 실패 시 대체 채널(현재 미정의)

### F-13 세션 종료
- **설명**: 헤더 [종료] → 확인 다이얼로그 → 페이지 새로고침
- **예외 처리**: '계속 진행' 시 다이얼로그만 닫힘

### F-14 차량 정보 수정(세션 중)
- **설명**: 헤더 ✎ 버튼으로 언제든지 차량 정보 재입력
- **비즈니스 규칙(요구사항)**: 수정 시 이후 단계 데이터 일관성 보장 필요(현재는 표시만 갱신)

---

## 5. 사용자 플로우

### 5.1 메인 등록(렌트 신청) 플로우
```
[Start]
  → 차량번호 입력 → (자동조회 OK?) ── No → 차량 직접 입력
                                       │
                                       Yes
                                       ↓
  → 차량 정보 확인 → 분기(렌트?) ── 교통비 조회 → 5.2로 우회
                                       │
                                       Rent
                                       ↓
  → 지역 선택 → 연락처 입력 + SMS 인증 → 추가 요청(선택)
  → 수리점 분기 ── 정함 → 공업사명 입력
                  └ 미정 → 협력 공업사 리스트 → 선택 → 예약 카드
  → 약관 동의 → 완료 카드 → 알림톡 발송
[End]
```

### 5.2 조회(교통비 시뮬레이션) 플로우
```
분기(교통비)
  → 수리 기간 선택(1~25일)
  → 과실 비율 선택(0~100%)
  → daily = 32000 × (100 - faultRate)/100 산출
  → 결과 표시
  → 분기(렌트로 변경?)
       ├ Yes → 5.1의 '지역 선택'으로 합류
       └ No → 종료 인사
```

### 5.3 수정 플로우
- **차량 정보**: 헤더 ✎ → 시트 A 재호출 → 4필드 수정 → 헤더 plate 갱신
- **지역**: 공업사 리스트에서 [지역 변경하기] → 시트 C 재호출 → 리스트 즉시 갱신

### 5.4 취소/이탈 플로우
- **세션 종료**: 헤더 [종료] → 다이얼로그 → 확정 시 페이지 리로드
- **시트 이탈**: Sheet B/C는 [X] 닫기 가능(미선택 상태 유지)

### 5.5 검색/필터 플로우(현재 부재 — 개선 항목)
- 협력 공업사 리스트에 거리/전문분야 필터 없음 → 11장에서 개선 제안

### 5.6 상태 변경 플로우(서버 측 예상)
```
신청 생성(Created)
  → 업체 배정 대기(Pending)
  → 업체 배정 완료(Assigned, 알림톡 전송)
  → 차량 인도(Delivered)
  → 반납/완료(Completed)
취소(Cancelled)는 어느 단계에서나 가능
```

### 5.7 완료 처리/취소 처리
- **완료 처리**: 클라이언트 측은 완료 카드 노출까지. 이후 단계는 백오피스/업체 시스템에서 진행.
- **취소 처리**: 챗봇 내 취소 UI 없음. 안내 문구(예약 정보 카드/완료 카드)로 "1588-0000 또는 배정된 업체"에 전화 안내.

---

## 6. 데이터 구조 정의 (ERD 수준)

### 6.1 엔티티 목록
1. **User (고객)**
2. **Vehicle (사고 차량)**
3. **ClaimSession (셀프클레임 세션)**
4. **TransitCostEstimate (교통비 시뮬레이션 기록)**
5. **RentalRequest (렌터카 신청)**
6. **Shop (협력 공업사)**
7. **Region (지역)**
8. **TermsAgreement (약관 동의)**
9. **PhoneVerification (SMS 인증)**

### 6.2 필드 정의

#### Vehicle
| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| plate | string | ✓ | 차량번호, 정규식 `^\d{2,3}[가-힣]\d{4}$` |
| name | string | ✓ | 차량명(예: 셀토스) |
| cls | enum | ✓ | 차종: 승용/SUV(RV)/승합/화물 |
| cc | enum | ✓ | 1600 미만 / 1600~1999 / 2000~2499 / 2500~2999 / 3000 이상 |
| year | enum | ✓ | 2018년 이전/이후 출고 |
| source | enum | - | auto(자동조회) / manual(직접입력) |

#### ClaimSession
| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| sessionId | uuid | ✓ | 세션 식별자 |
| userId | fk | ✓ | User |
| vehicleId | fk | ✓ | Vehicle |
| branch | enum | ✓ | rent / estimate |
| phone | string | rent 시 ✓ | 본인확인 통과 번호 |
| regionCode | string | rent 시 ✓ | 예: SEO_GWANGJIN |
| additionalNote | string | - | ≤100자 |
| garageDecided | bool | rent 시 ✓ | 정함/미정 |
| shopRef | fk \| string | rent 시 ✓ | Shop FK 또는 사용자 입력명 |
| status | enum | ✓ | 7장 상태 참조 |
| createdAt, updatedAt | ts | ✓ | |

#### TransitCostEstimate
| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| estimateId | uuid | ✓ | |
| sessionId | fk | ✓ | |
| repairDays | int | ✓ | 1~25 |
| faultRate | int | ✓ | 0..100 (step 10) |
| dailyAmount | int | ✓ | 산출값 |
| baseDailyRate | int | ✓ | 현재 32,000 (정책 테이블화 권장) |

#### RentalRequest
| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| requestId | uuid | ✓ | |
| sessionId | fk | ✓ | |
| status | enum | ✓ | Created/Pending/Assigned/Delivered/Completed/Cancelled |
| assignedVendorId | fk | - | 배정 렌터카 업체 |
| termsAgreementId | fk | ✓ | |

#### Shop (협력 공업사)
| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| shopId | uuid | ✓ | |
| name | string | ✓ | |
| addr | string | ✓ | |
| regionCode | fk | ✓ | |
| distanceMeters | int | - | 사용자 위치 기준 |
| specialty | string | - | 전문 분야 |
| phone | string | ✓ | |
| isCertified | bool | ✓ | 보험사 인증 여부 |

#### Region
| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| regionCode | string | ✓ | PK |
| sido | string | ✓ | 시도 |
| sigungu | string | ✓ | 구/군 |
| isServiceable | bool | ✓ | 서비스 가능 여부 |

#### TermsAgreement
| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| agreementId | uuid | ✓ | |
| sessionId | fk | ✓ | |
| over21 | bool | ✓ | 그룹1-1 |
| termsAndPrivacy | bool | ✓ | 그룹1-2 |
| restrictionAck | bool | ✓ | 그룹2-1 |
| agreedAt | ts | ✓ | |

#### PhoneVerification
| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| verificationId | uuid | ✓ | |
| phone | string | ✓ | |
| codeHash | string | ✓ | |
| expiresAt | ts | ✓ | issuedAt + 180s |
| verifiedAt | ts | - | |

### 6.3 관계 구조
```
User 1 ── * ClaimSession
ClaimSession 1 ── 1 Vehicle
ClaimSession 1 ── 0..* TransitCostEstimate   (조회 분기 진입 시 1건 이상)
ClaimSession 1 ── 0..1 RentalRequest        (렌트 분기 완료 시)
RentalRequest 1 ── 1 TermsAgreement
RentalRequest 0..1 ── 0..1 Shop              (shopRef가 FK일 때)
ClaimSession 0..* ── 1 Region                (regionCode)
ClaimSession 1 ── 0..* PhoneVerification     (재발송 N회)
```

---

## 7. 상태(State) 정의

### 7.1 ClaimSession 상태
| 상태 | 의미 | 진입 조건 | 종료 조건 | 다음 가능한 상태 |
|---|---|---|---|---|
| Started | 세션 시작 | 세션 진입 | 차량번호 제출 | VehicleResolved, Exited |
| VehicleResolved | 차량 정보 확정 | 자동조회 성공 또는 직접 입력 완료 | 분기 선택 | BranchRent, BranchEstimate, Exited |
| BranchEstimate | 시뮬레이션 분기 | 'end' 선택 | 결과 노출 후 사용자 응답 | BranchRent(전환), Exited |
| BranchRent | 렌트 신청 분기 | 'rent' 직진/전환 | 약관 동의 완료 | Submitting |
| Submitting | 최종 제출 처리 | 약관 동의 후 | 완료 카드 표시 | Completed, Failed |
| Completed | 신청 완료 | 완료 카드 노출 | (세션 종료) | (terminal) |
| Failed | 제출 실패 | 서버 오류 | 재시도/종료 | BranchRent, Exited |
| Exited | 사용자 종료 | [종료] 확정 | (페이지 리로드) | (terminal) |

### 7.2 PhoneVerification 상태
| 상태 | 의미 | 진입 조건 | 종료 조건 | 다음 가능한 상태 |
|---|---|---|---|---|
| Idle | 미발송 | 화면 진입 | 발송 요청 | CodeSent |
| CodeSent | 발송됨/대기 | [인증번호 받기] | 180s 만료 또는 검증 | CodeExpired, Verified, CodeSent(재발송) |
| Verified | 검증 성공 | 코드 일치 | (다음 단계 이동) | (terminal) |
| CodeExpired | 만료 | 180s 경과 | 재발송 | CodeSent |

### 7.3 RentalRequest 상태(서버 측 라이프사이클 권장)
| 상태 | 의미 | 진입 | 종료 | 다음 |
|---|---|---|---|---|
| Created | 신청 생성 | 사용자 신청 완료 | 업체 배정 시작 | Pending |
| Pending | 업체 배정 대기 | 워크플로우 트리거 | 배정 확정 | Assigned, Cancelled |
| Assigned | 업체 배정 | 배정 확정 + 알림톡 | 차량 인도 | Delivered, Cancelled |
| Delivered | 차량 인도 완료 | 업체 보고 | 반납 | Completed, Cancelled |
| Completed | 종료 | 반납 처리 | - | (terminal) |
| Cancelled | 취소 | 사용자/업체 요청 | - | (terminal) |

---

## 8. API 요구사항

### 8.1 차량 조회
- **POST** `/api/v1/vehicles/lookup`
- **Request**: `{ "plate": "12가3456" }`
- **Response 200**: `{ "plate", "name", "cls", "cc", "year", "source":"auto" }`
- **Response 404**: `{ "code":"VEHICLE_NOT_FOUND" }`
- **권한**: 세션 토큰(고객 본인 인증 후 발급)

### 8.2 차량 직접 입력 저장
- **PUT** `/api/v1/sessions/{sid}/vehicle`
- **Request**: `{ plate, name, cls, cc, year, source:"manual" }`
- **권한**: 세션 토큰

### 8.3 교통비 시뮬레이션
- **POST** `/api/v1/transit-cost/estimate`
- **Request**: `{ sessionId, repairDays, faultRate }`
- **Response 200**: `{ dailyAmount, baseDailyRate, formula }`
- **권한**: 세션 토큰

### 8.4 지역(시도/구) 조회
- **GET** `/api/v1/regions?sido=SEO`
- **Response 200**: `[{ regionCode, sigungu, isServiceable }, ...]`
- **권한**: 공개

### 8.5 SMS 인증번호 발송
- **POST** `/api/v1/auth/phone/code`
- **Request**: `{ sessionId, phone }`
- **Response 200**: `{ verificationId, expiresIn:180 }`
- **권한**: 세션 토큰 + 레이트리밋(분당 N회)

### 8.6 SMS 인증번호 검증
- **POST** `/api/v1/auth/phone/verify`
- **Request**: `{ verificationId, code }`
- **Response 200**: `{ verified:true }` / **400**: `INVALID_CODE | EXPIRED`
- **권한**: 세션 토큰

### 8.7 협력 공업사 조회
- **GET** `/api/v1/shops?regionCode=SEO_GWANGJIN&limit=2&cursor=...`
- **Response 200**: `{ items:[Shop...], nextCursor }`
- **권한**: 세션 토큰

### 8.8 렌터카 신청 생성
- **POST** `/api/v1/rental-requests`
- **Request**:
```json
{
  "sessionId": "...",
  "regionCode": "SEO_GWANGJIN",
  "phone": "010-...",
  "additionalNote": "string|null",
  "garageDecided": true,
  "shopId": "uuid|null",
  "shopNameInput": "string|null",
  "agreement": { "over21":true, "termsAndPrivacy":true, "restrictionAck":true }
}
```
- **Response 201**: `{ requestId, status:"Created" }`
- **Response 409**: `STATE_CONFLICT` (중복 신청)
- **권한**: 세션 토큰 + SMS 인증 완료 상태

### 8.9 세션 종료
- **DELETE** `/api/v1/sessions/{sid}`
- **권한**: 세션 토큰

### 8.10 알림톡 발송(내부)
- **POST(internal)** `/api/v1/notifications/kakao`
- 신청 완료 직후 트리거. 실패 시 SMS 폴백.

---

## 9. 예외 및 오류 시나리오

| 시나리오 | 발생 위치 | 시스템 처리 |
|---|---|---|
| 빈 데이터 | 차량명/번호/지역 등 | 다음 액션 버튼 비활성 + 인라인 가이드 메시지 |
| 검색 결과 없음 | 차량 자동조회, 협력 공업사 추천 | 차량: 직접 입력 시트로 전환 / 공업사: "현재 지역에 추천 가능한 공업사가 없어요. 지역을 변경해 주세요" 안내 + [지역 변경] 노출(현재 미구현) |
| 네트워크 오류 | 모든 API | "잠시 후 다시 시도해 주세요" 메시지 + 재시도 버튼. 챗 흐름 상 마지막 메시지 단계로 되돌림(현재 미구현) |
| 권한 없음 | 세션 만료/미인증 | 첫 화면(차량번호 입력)으로 리셋 + 안내 메시지 |
| 필수값 누락 | 차량 직접 입력, 약관, 인증 미완료 | 버튼 비활성으로 차단 |
| 상태 충돌 | 중복 신청, 이미 신청된 세션에서 재신청 | 409 → "이미 신청이 접수되었어요. 변경/취소는 1588-0000으로 문의해 주세요" |
| 중복 처리(SMS 코드 재발송 폭주) | F-06 | 분당 발송 횟수 제한(권장 3회/분), 초과 시 안내 |
| 인증번호 만료 | F-06 | "유효 시간이 지났어요. 재발송해 주세요" + 재발송 활성 |
| 약관 미동의 | S11 | [렌트 신청하기] 비활성 유지 |
| 알림톡 발송 실패 | F-12 | SMS 폴백 + 완료 카드 내 표시 변경(현재 미구현) |
| 차량 정보 수정 후 데이터 정합성 | F-14 | 이후 단계에 영향 시(예: 차량 종류 변경) 시뮬레이션/배정 재계산 트리거 필요(현재 표시만 갱신) |

---

## 10. 컴포넌트 구조 정의(기능 단위)

### 10.1 레이아웃 컴포넌트
- **HeaderBar** — 로고/차량바/종료 버튼 컨테이너
- **VehicleBadge** — 헤더 내 현재 차량 plate 표시 + 수정 트리거(✎)
- **ChatViewport** — 챗 스크롤 영역, 자동 스크롤 책임

### 10.2 메시지 컴포넌트
- **BotMessageBubble** — 봇 메시지 출력(typing → 메시지)
- **UserMessageBubble** — 사용자 입력/선택 결과 에코
- **HintMessage** — 진행 단계 힌트
- **ActionPillButton** — 시트 호출용 단일 버튼
- **QuickChoiceGroup** — 2~N개 선택지 버튼 그룹(단일 선택 후 자동 사라짐)
- **TypingIndicator** — 봇 응답 대기 표시

### 10.3 입력 폼 컴포넌트
- **PlateInputCard** — 차량번호 입력 + 검증 + 조회 버튼
- **InlineContactForm** — 휴대폰 + SMS 인증 + 완료 (F-06)
- **InlineRequestForm** — 자유 텍스트 폼(글자수 카운터)
- **InlineShopNameForm** — 공업사명 단일 입력

### 10.4 카드 컴포넌트
- **ShopCardList** — 협력 공업사 카드 리스트 (단일 선택 + 더보기 + 지역 변경)
- **ShopCard** — 단일 공업사 카드(인증 뱃지, 거리, 전문분야)
- **ReserveInfoCard** — 예약 정보 요약 카드
- **CompletionCard** — 최종 완료 카드

### 10.5 시트(모달) 컴포넌트
- **BottomSheet** — 공통 컨테이너(헤더/바디/푸터 슬롯)
- **VehicleEditSheet** — 차량 4필드 직접 입력
- **RegionSelectSheet** — 지역 칩 선택
- **DaysSelectSheet** — 1~25일 칩 선택 (수리 기간/과실비율 공용 — 현재 동일 시트 재활용)
- **TermsSheet** — 약관 3건 + 전체 동의
- **ExitConfirmDialog** — 종료 확인

### 10.6 보조 컴포넌트
- **ChipGroup** — 단일 선택 칩 그룹(지역/일수/과실 공용)
- **CountdownTimer** — 인증 180초 카운트다운
- **CharCounter** — 글자수 카운터

### 10.7 도메인 책임
- **SessionStore** (현 `S` 객체) — region/shopData/phone/vehInfo/days/faultRate 상태 보관
- **ChatController** — 메시지 추가/타이핑/스크롤 책임
- **SheetController** — 오버레이/시트 open/close 책임

---

## 11. 개선 제안

### 11.1 기능 중복 / 통합 가능 지점
1. **`#sheetDays` 한 시트의 이중 용도** — 수리 기간 선택과 과실 비율 선택이 동일 시트를 재활용하여 헤더 텍스트만 바꿈. 의미가 완전히 다르므로 **별도 컴포넌트로 분리**하거나, 적어도 별도 instance/상태로 관리해야 한다(현재 `daysResolve`/`sheetOptionValue` 전역 공유로 동시성 버그 위험).
2. **차량번호 정규식 분기** — 조회 실패를 `plate.startsWith('99')`라는 mock 규칙으로 처리. 실제 API 도입 시 명시적 응답 코드(404/422) 분기로 정리 필요.
3. **공업사 추천 데이터 생성 로직** — `getShopsByRegion`이 지역 prefix 문자열 치환 mock. 실데이터 연동 시 페이지네이션·커서·거리 정렬 기준 정의 필요.

### 11.2 화면 구조 문제
1. **헤더 차량바와 본문 차량 정보 메시지의 정보 중복** — 자동조회 후 봇 메시지로 4필드를 나열하고, 동시에 헤더에도 plate를 띄움. 헤더의 ✎ 수정 결과가 챗 메시지 본문에는 반영되지 않음(불일치 위험). **단일 source of truth(SessionStore) 기준으로 챗 메시지 본문도 재렌더링** 또는 본문 메시지를 카드 형태로 분리 후 재렌더 가능하게 변경.
2. **시트 닫기 동선 불균일** — Region/Days 시트만 [X] 노출, Vehicle/Terms 시트는 닫기 버튼 없음(오버레이 탭으로만 닫힘). 닫기 정책 통일 필요(특히 Vehicle 시트는 진입 경로가 폴백이므로 닫으면 챗 흐름이 멈플 위험).
3. **시·구 선택의 비대칭** — 서울 외 시(부산/대구/인천)는 클릭 동작이 없는 헤더만 노출. 실제 서비스 범위를 IA로 명시하거나, 미지원 지역은 안내 메시지로 차단해야 한다.

### 11.3 사용자 플로우 문제
1. **세션 종료 = 페이지 리로드** — `location.reload()`로 처리. 입력 도중 종료 시 모든 데이터 소실, 복귀 불가. **세션 임시 저장 + 재개(Resume)** 기능 필요. 사고 직후 상황 특성상 중단/재개 시나리오는 빈번할 수 있다.
2. **시뮬레이션 → 렌트 전환 시 입력 재사용 부족** — 시뮬레이션에서 입력한 수리 기간/과실은 RentalRequest 단계에서 사용되지 않고 버려짐. 렌트 신청에 동일 정보가 필요하다면 캐리오버 필요(또는 명확히 분리).
3. **차량 정보 수정 시 후속 단계 영향 처리 누락** — 헤더 ✎로 차량 정보 변경 시 표시만 갱신되고 이후 단계(시뮬레이션/배정 비용)에 재반영 없음. 변경 시점에 따라 재계산·재확인 워크플로우가 필요하다.
4. **수리점 직접 입력 시 검증 부재** — `S9-A`에서 자유 텍스트로만 받음. 실제 협력 업체 매칭 가능성이 낮아 운영 과부하 유발. **자동완성/주소 결합 입력** 필요.
5. **추가 요청사항의 사용처 불명** — 입력 후 봇 메시지만 노출되고 실제 어디로 전달되는지(렌트 업체/수리점/보험사) 불명. 데이터 라우팅 명시 필요.

### 11.4 확장성 문제
1. **상태 관리의 전역 변수 의존** — `S` 객체와 `daysResolve`/`rgResolve` 등 전역 resolver로 비동기 흐름을 제어. 동시 입력·취소·재진입 등 에지케이스에서 의도치 않은 resolve 위험. **상태 머신(예: XState)** 또는 명시적 컨트롤러 패턴 도입 권장.
2. **하드코딩 정책값(32,000원/일, 25일 한도 등)** — 정책 변경 시 코드 수정 필요. **정책 테이블/원격 컨피그** 분리 권장.
3. **다국어/접근성 미고려** — 메시지가 HTML 문자열에 인라인. i18n·스크린리더 친화 구조로 추출 필요.
4. **챗 흐름의 비순환 가정** — 거의 모든 분기가 forward-only. "이전 단계로" 또는 "마지막 답변 수정"이 불가. 사용자 실수 보정 동선 추가 권장(예: 잘못 선택한 지역 변경).
5. **에러/네트워크 처리 부재** — 현재 mock 환경이라 실패 분기 자체가 없음. 실 API 연동 시 모든 비동기 호출에 재시도·타임아웃·폴백 메시지 정의 필요.
6. **약관 본문 보기 미구현** — 그룹1의 "보기" 링크와 그룹2의 "보기" 링크가 동작하지 않음. 약관 원문 페이지/시트 별도 정의 필요.
7. **취소 셀프 처리 부재** — 신청 후 챗봇 내에서 취소가 불가능하고 전화 안내만 존재. 셀프 취소 플로우 추가 시 사용자 만족도 개선 여지 큼.

### 11.5 우선 개선 권장 순위
1. **Phase 1 (필수)**: 세션 임시 저장/재개, 에러 처리, SMS 레이트리밋, 약관 본문 뷰
2. **Phase 2 (중요)**: 상태 머신 도입, 정책값 외부화, 차량 수정 후 재계산, 시·구 서비스 범위 정의
3. **Phase 3 (확장)**: 셀프 취소, 공업사 검색/필터, 추천 0건 대응, 다국어/접근성

---

> 본 PRD는 현 HTML 목업의 **기능·구조·플로우만**을 기준으로 작성되었으며, 시각적 디자인 요소는 분석에서 제외되었습니다. 실제 개발 착수 시 8장 API 스펙은 백엔드와 협의하여 확정 필요합니다.
